export default function NewReturnRequestPage() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-12">
      <h1 className="text-3xl font-semibold">Request a return</h1>
      <p className="mt-3 text-sm leading-6 text-black/60">
        Return request intake should validate order ownership, item eligibility, return window, quantity, and requested action.
      </p>
    </main>
  );
}
