# Service Products

Service products can capture service type, duration, delivery window, required customer brief, scheduling requirement, location type, meeting URL, deliverables, and revision limits.

After verified payment, order completion creates a `ServiceOrder`. Admin service pages are available at `/admin/services`, and customer service pages are available at `/account/services`.

Service products do not require shipping by default.
