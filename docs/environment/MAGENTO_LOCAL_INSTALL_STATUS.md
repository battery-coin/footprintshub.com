# Magento Local Install Status

Audit date: 2026-05-08

Magento Open Source has been installed locally in this repository for development.

## Confirmed

- Branch: `magento-local-readiness`
- Magento CLI: `Magento CLI dev-main`
- Composer dependencies: installed locally in ignored `vendor/`
- Database: `footprintshub_magento`
- Database user: `footprintshub` at `localhost`
- Search engine: OpenSearch at `http://localhost:9200`
- OpenSearch version: 2.19.5
- Setup install: completed
- Cache flush: completed
- Indexer reindex: completed
- Module status: completed

## Local Admin

Admin URI generated by Magento:

```text
/admin_gz0jb71
```

After the Laragon virtual host is configured, the local admin URL should be:

```text
http://footprintshub.test/admin_gz0jb71
```

## Not Yet Verified

- Laragon virtual host serving `pub`
- Storefront browser load
- Admin browser load
- Checkout flow
- Product page flow

## Important Git Safety

Do not commit:

- `app/etc/env.php`
- `app/etc/config.php`
- `auth.json`
- `.env`
- `vendor/`
- `var/`
- `generated/`
- generated `pub/static` files
- generated/user `pub/media` files
- database dumps
- source archives such as `magento2-2.4-develop.zip`
