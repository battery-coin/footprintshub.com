# Token Payment Asset Settings

Admin route:

- `/admin/settings/tokens`

Owner-configurable public fields:

- token name
- symbol
- chain
- contract address or public asset ID
- decimals
- enabled flag
- utility description

Do not store:

- private keys
- seed phrases
- wallet recovery phrases
- payout credentials

Battery Coin is described only as a utility token payment option.
